[project]
name = "koke"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.12.0",
    "pyyaml>=6.0.2",
    "requests>=2.31.0",
    "unidecode>=1.4.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
